# 토스쇼핑 쉐어링크 조사 (2026-07-28)

딜방에 쿠팡 링크와 토스쇼핑 링크가 섞여 올라오는 것이 확인되어,
토스쇼핑으로도 수익을 낼 수 있는지 공식 문서를 조사했다.

**조사 결과 한 줄 요약: 쉐어링크에는 공개 API가 없다.
링크 생성은 토스 앱/웹의 공유 UI 를 거쳐야만 한다.**

---

## 문서가 두 개로 나뉘어 있다

처음 지목된 `shopping-docs.toss.im/dev` 는 **제휴와 무관한 셀러용 API** 였다.

| 문서 | 대상 | 내용 |
|---|---|---|
| `shopping-docs.toss.im` | **셀러(판매자)** | 상품 등록, 주문 처리, 재고·배송 연동 |
| `sharelink-docs.toss.im` | **크리에이터(제휴)** | 쉐어링크 발급, 리포트, 정산 ← 우리가 원하는 쪽 |

셀러 API 에는 어필리에이트 링크 생성 기능이 없다. 상품을 파는 쪽의 API 다.

---

## 쉐어링크 링크 생성 방법 (공식 문서 기준)

```
토스쇼핑에서 상품 상세 페이지로 이동
  → 상단 공유 아이콘 탭
  → '쉐어링크 공유하기' 선택
  → 링크 발급
```

문서에 **없는** 것:

- 상품 URL 을 붙여넣어 링크를 만드는 방식
- 개발자용 API / 엔드포인트 / 인증 방식
- 프로그래매틱(자동) 링크 생성 수단

`llms.txt` 문서 인덱스 전체(11개 항목: 링크 만들기, 리포트, 실적 대시보드,
수익금 계산, 정산 내역, 정산액 지급, 세금계산서, 공지사항, 운영 정책, FAQ)에
개발/연동/API 항목이 존재하지 않는다.

### ⚠️ 일반 공유 링크는 수익이 0 이다

> "일반 공유 메뉴(`공유하기`)로 만든 링크는 수익이 발생하지 않아요."

반드시 `쉐어링크 공유하기` 로 만든 링크여야 한다.
**CLAUDE.md 제약 4번(손으로 조립한 링크는 트래킹이 안 붙는다)과 같은 함정이다.**
토스에도 그대로 적용된다.

---

## 운영 정책에서 확인한 것

### 대가성 고지 — 쿠팡과 문구도 위치 규정도 다르다

> "이 포스팅은 토스쇼핑 쉐어링크 활동의 일환으로, 이에 따른 일정액의 수수료를 제공받습니다."

- **모든 게시물에 빠짐없이** 포함
- **게시물 제목이나 첫 부분에** 명확히 노출

쿠팡 고지는 `threads_post.py` 에서 본문 **끝**에 붙이고 있다.
토스는 **첫 부분** 을 요구하므로, 토스 링크를 발행하려면 문구 배치 규칙이
링크 종류에 따라 달라져야 한다. 두 고지 문구를 혼용해서도 안 된다.

### 자동화 관련 조항

> "무효 클릭, 자동 실행, 과도하게 클릭을 유도하는 등 사용자의 의도에 반하는
> 방식은 모두 금지"

문맥상 **클릭 조작(클릭 어뷰징) 금지**로 읽힌다. 링크를 자동으로 생성하는
행위를 명시적으로 금지한 조항은 아니다. 다만 매크로·크롤링에 대한 별도
허용 조항도 없다. 회색지대다.

위반 시 **수익금 지급 중단 또는 계약 해지**.

### 가입 조건과 수수료 — 공식 FAQ 확인

> "링크를 공유할 수 있는 분이라면 누구나 가능해요.
>  홈페이지, 블로그, SNS 등 자신이 운영하는 채널에서 바로 시작할 수 있어요."

| 항목 | 값 | 출처 |
|---|---|---|
| 가입 자격 | 채널만 있으면 누구나. 사업자/개인 모두 | FAQ |
| 수수료 | **링크 접속 후 24시간 내 구매액의 10%** | FAQ |
| 월 최대 수익금 | 3,000만 원 | FAQ |
| 지급일 | 다음 달 25일 | FAQ |
| 고지 의무 | "모든 게시물에는 경제적 이해관계를 꼭 표시" | FAQ |

계약 이행 과정에서 필요서류 제출과 세금계산서 역발행 승인이 요구된다.

### 쿠팡과 비교하면 진입 장벽이 훨씬 낮다

| | 쿠팡 파트너스 | 토스 쉐어링크 |
|---|---|---|
| 가입 | 승인 필요 | 채널만 있으면 누구나 |
| 수수료 | 3~5% | **10%** (24시간 창) |
| 링크 생성 API | 최종승인 + 누적 판매 15만 원 필요 | 공개 API 없음 |

**수수료가 2~3배이고 가입 장벽이 사실상 없다.**
CLAUDE.md 의 Phase 계획은 쿠팡 API 승인을 최종 목표로 두고 있는데,
토스 쪽이 더 빨리 수익이 날 가능성이 있다. 단, 링크 생성 자동화 수단이
확인되어야 한다는 전제가 붙는다.

---

## 이 프로젝트에 주는 의미

이 방은 **상품 발굴 소스**다. 이미 최저가로 걸러진 상품 리스트를 얻는 곳이지
링크를 얻는 곳이 아니다. 방에 올라온 링크는 방장의 것이므로, 상품만 알아내고
링크는 내 것으로 새로 만든다. 쿠팡에서 이미 그렇게 설계되어 있다.

```
방의 단축링크 → 실제 상품 URL + 상품ID 확정 → 내 링크 생성 → 발행
```

### 단축링크 해석 — 실측 검증 완료 (2026-07-28)

`toss.im/_m/...` 는 리다이렉트 1회로 풀린다.

```
https://toss.im/_m/9X1XXhQf
  ↓
https://toss.shopping/t/524516537?k=99af7d09-fc31-4a94-99f1-e6a567ddbce5&referrer=affiliate
```

| 조각 | 의미 |
|---|---|
| `toss.shopping/t/(\d+)` | **상품 ID.** 쿠팡의 `/vp/products/(\d+)` 에 대응한다 |
| `k=<uuid>` | **방장의 트래킹 키.** 반드시 버린다 |
| `referrer=affiliate` | 제휴 링크임을 나타냄. 버린다 |

파라미터를 제거한 `https://toss.shopping/t/524516537` 로 접속하면 정상
동작하고(HTTP 200), 토스가 스스로 이 주소를 정식 URL 로 선언한다.

```html
<meta property="og:url"   content="https://toss.shopping/t/524516537">
<meta property="og:title" content="이장네푸드 전통압착 통들기름 100%, 350ml, 4개 | 토스쇼핑">
<meta property="og:description" content="... 들기름 1위. 리뷰 185개 · 평점 4.7점">
```

**상품명을 `og:title` 에서 그대로 얻을 수 있다.** 채팅 메시지를 추측 파싱하는
`guess_title()` 보다 정확하다. 토스 딜은 이 경로를 쓰는 게 낫다.
(`| 토스쇼핑` 접미사는 떼어낼 것.)

### 남은 병목은 링크 생성 한 곳뿐이다

| 단계 | 토스 | 근거 |
|---|---|---|
| 딜방에서 토스 링크 추출 | ✅ | 정규식 추가만 하면 됨 |
| 단축링크 → 상품 URL + 상품ID | ✅ **검증됨** | 위 실측 |
| 상품명·가격 확보 | ✅ **검증됨** | `og:title` / `og:description` |
| **내 쉐어링크 생성** | 🔴 **미해결** | 공개 API 없음 |
| Threads 발행 | ✅ | 단 고지를 첫 부분에 둬야 함 |

문서상 링크 생성 절차가 "상품 상세 페이지 → 공유 아이콘 → 쉐어링크 공유하기"
인데, **이제 그 상품 상세 페이지 URL 을 프로그램으로 만들 수 있다.**
따라서 `partners_link.py` 와 같은 방식(로그인 세션을 가진 브라우저로 해당
상품 페이지를 열고 공유 버튼을 누르는 것)이 성립할 가능성이 높다.
확인이 필요한 것은 그 페이지에서 공유 UI 가 실제로 노출되는지 하나다.

---

## PC 웹 확인 결과 — 공유 UI 없음 (2026-07-28 실측)

`https://toss.shopping/t/524516537` 을 PC 크롬으로 열어 확인했다.

**보이는 것**: 상품 이미지, 상품명, 평점·리뷰(185개), 배송일정, 판매자
**없는 것**: **가격**, 공유 아이콘, '쉐어링크 공유하기'

화면 하단에 **`앱에서 가격 확인하기`** 버튼이 고정으로 붙어 있다.
가격조차 웹에 노출하지 않고 앱으로 유도한다.
즉 **토스쇼핑 웹은 미리보기/SEO 용 페이지이고 실기능은 앱 전용이다.**

데스크톱 PC 웹도 같다. 가격 자리에 **"QR코드를 찍어 가격을 확인해보세요"** 가
있고 공유 버튼이 없다. 모바일이든 데스크톱이든 **상품 상세 페이지에서는**
링크를 만들 수 없다.

### ✅ 대시보드 실측 — PC 웹에서 링크 생성이 된다 (2026-07-29)

`tools/observe_toss.py` 로 로그인 상태의 `sharelink.toss.im/home` 을 덤프했다.

**로그인**: 개인 토스 앱 계정이 아니라 **토스 비즈니스 계정**을 쓴다.
`business.toss.im/account/sign-in` 으로 넘어가며 이메일/ID 또는 QR코드로
로그인한다. **PC 웹 로그인이 정식 경로다. 앱 전용이 아니다.**

**내비게이션**

```
홈 · 링크 · 베스트 랭킹 · 상품 조회 · 성과 · 실적 대시보드 · 정산 내역 · 내 정보 · 회원 정보
```

**`링크 발급` 버튼이 상품마다 있다** (홈 화면에서만 38개).
상품 카드에 특가율·개당 수익·가격·평점이 함께 표시된다.

```
67% 특가 / 개당 1,710원 수익
베베드 아기곰 저자극 아기 물티슈 캡형, 100매, 20개
17,100원  30일 최저가  4.5 (13)  오늘출발   [링크 발급]
```

**따라서 앞선 "PC 자동화 불가" 판정은 틀렸다.** 상품 상세 페이지
(`toss.shopping/t/...`)에 공유 버튼이 없다는 것과, 쉐어링크 대시보드에
링크 생성이 없다는 것은 다른 이야기였다. 대시보드에는 있다.

**수수료**: 화면에 "수수료 10% 프로모션 / 9월 25일까지 연장" 이 떠 있다.
FAQ 의 10% 와 일치하며 종료 예정일이 명시되어 있다. 영구 요율이 아니다.

**남은 확인**: 홈 화면에는 입력창이 0개다. 특정 상품을 이름으로 찾으려면
`상품 조회` 화면을 봐야 한다. 딜방에서 얻은 상품을 지정해 링크를 만들려면
이 화면의 검색 UI 가 필요하다.

### 전용 웹 서비스 발견 경위

`https://sharelink.toss.im/` 이 **HTTP 200 으로 살아 있다.**

```html
<title>토스쇼핑 쉐어링크</title>
<meta property="og:description"
      content="토스쇼핑의 상품을 공유하고, 구매가 발생하면 수수료를 받을 수 있는 제휴마케팅 서비스예요.">
```

가입은 `https://sharelink.toss.im/signup`.
문서의 '링크 만들기 / 리포트 / 실적 대시보드 / 정산 내역' 이 이 서비스의
메뉴로 보인다.

**중요한 구분**: "상품 상세 페이지에 공유 버튼이 없다" 와 "쉐어링크
대시보드에 링크 생성 기능이 없다" 는 서로 다른 이야기다. 앞의 것은
확인됐지만 뒤의 것은 아직 확인되지 않았다.
대시보드에 상품 검색 + 링크 생성이 있다면 `partners_link.py` 와 같은
방식이 그대로 성립한다. 상품명은 `og:title` 로 이미 정확히 얻을 수 있으므로
그것을 검색어로 쓰면 된다.

이 페이지는 JS 로 그려지는 SPA 라 정적 HTML 에 경로가 남지 않는다.
**로그인한 브라우저로 직접 열어봐야만 알 수 있다.**

| 수단 | 판정 |
|---|---|
| 상품 상세 페이지에서 공유 | 🔴 불가. 공유 UI 자체가 없음 (모바일/데스크톱 모두) |
| **`sharelink.toss.im` 대시보드에서 생성** | ⬜ **미확인 — 이게 관건이다** |
| 공개 API | 🔴 없음 (문서 전체 확인) |
| 안드로이드 에뮬레이터 자동화 | 🟡 이론상 가능하나 무겁고 계정 리스크가 크다. 권장하지 않음 |
| 사람이 앱에서 수동 생성 | 🟢 가능하지만 무인 운영이 아니게 된다 |

### 그래서 지금의 권장 방향

1. **쿠팡 자동화를 먼저 완성한다** (T1~T5). 설계 변경 없음.
2. **토스 딜도 `deals.db` 에는 쌓아 둔다.** 추출·상품URL해석·상품명까지는
   이미 검증됐고 비용이 거의 없다. `affiliate_url` 이 NULL 인 채로 큐에
   남으므로, 나중에 수단이 생기면 그대로 이어서 처리할 수 있고 지금도
   수동 처리용 목록이 된다. **단 링크 없이 발행되지 않도록 막아야 한다.**
3. **토스에 API 유무를 문의한다.** `sharelink@toss.im`.
   비공개 파트너 API 가 있을 수 있고 비용이 0 이다.

### 주의: 가격은 웹에서 못 얻는다

쿠팡과 달리 토스는 상품 페이지에 가격이 없다. `og:description` 에도 없다.
토스 딜의 가격은 **채팅 메시지에서 파싱한 값**(`guess_price()`)이 유일한
출처다. 방 메시지에 "역대 최저가 23,800원" 처럼 적혀 있으므로 가능은 하다.

## 확인 완료 (2026-07-29)

- [x] 토스쇼핑 쉐어링크 가입 — 완료
- [x] 대시보드에서 링크 생성 가능한가 — **가능하다.** 발급 검증 완료
- [x] 생성된 링크 형태 — `toss.im/_m/...`
- [x] **대시보드에 상품 검색 기능이 있는가 — 🔴 없다. 확정**
- [ ] `sharelink@toss.im` 에 API 문의 → 회신 내용

### 🔴 PC 웹에는 검색이 없다 — 확정 (2026-07-29)

소유자가 **토스 앱에서는 검색이 되고, 상품 상세의 공유 아이콘에
'쉐어링크 공유하기' 메뉴가 있다**는 것을 스크린샷으로 확인해 주었다.
그래서 PC 웹에서도 같은 경로가 열리는지 다시 봤다. 결과는 전부 막혔다.

**1. 상품 상세 페이지 — 로그인 상태 + 모바일 UA 로도 공유 UI 가 없다**

이전 관측은 로그아웃 상태의 PC 웹이었으므로, 크리에이터 로그인 세션을
가진 채로 다시 확인했다. UA 를 세 가지로 바꿔 봤다.

| UA | 공유 관련 요소 |
|---|---|
| PC 기본 | **0개** |
| iPhone Safari | **0개** |
| 토스앱 위장 (`TossApp/5.0`) | **0개** |

가격도 여전히 안 나온다. `toss.shopping` 웹은 SEO·미리보기 전용이 맞다.

**2. 쉐어링크 대시보드 — 어느 화면에도 텍스트 입력창이 없다**

8개 경로를 훑었다. 전부 텍스트 입력창 0개다.

```
/home                        →  /home                      0개
/links/recommended-products  →  (그대로)                    0개
/links/best-ranking          →  (그대로)                    0개
/links/ranking               →  /home 으로 리다이렉트
/links                       →  /home
/links/search                →  /home
/products                    →  /home
/search                      →  /home
```

`/links/best-ranking` 은 실재하는 화면이지만 거기에도 검색은 없다.
`상품 조회` 는 `/links/recommended-products`(프로모션 상품)이고 필터만 있다.

### 그래서 결론

| 수단 | 판정 |
|---|---|
| 상품 상세 페이지에서 공유 | 🔴 불가. 로그인·모바일UA 모두 공유 UI 없음 |
| **대시보드에서 목록 골라 발급** | 🟢 **가능. 이걸 쓴다** (검증 완료) |
| 대시보드에서 상품 검색 | 🔴 **불가. 검색 UI 자체가 없음** |
| 공개 API | 🔴 없음 |
| 안드로이드 에뮬레이터 | 🟡 토스는 금융 앱이라 루팅·에뮬레이터 탐지가 강하다.<br>로그인부터 막힐 가능성이 높고, 뚫려도 금융 계정 리스크가 크다 |
| 실제 폰 + ADB 자동화 | 🟡 기술적으로 가능. 폰을 상시 연결해야 하고 앱 UI 변경에 취약 |

**딜방의 토스 딜은 내 링크를 만들 수 없다.** 상품을 지정할 방법이 없기
때문이다. 대신 대시보드가 주는 특가 목록(30일 최저가·특가율 표시)이
같은 성격의 핫딜이므로 그쪽에서 발급한다.

즉 두 플랫폼의 소스가 다르다.

```
쿠팡  : 딜방 메시지 → 상품 URL → 간편 링크 만들기   (실시간)
토스  : 쉐어링크 대시보드 목록 → 링크 발급          (주기적 하베스팅)
```

딜방의 토스 딜은 `affiliate_url` 이 NULL 인 채로 DB 에 남는다.
나중에 수단이 생기면 그대로 이어서 처리할 수 있다.

### 자동화가 가능해질 경우의 설계 (미리 정리)

상품 URL 로 변환하는 방식이 아니라 **상품명으로 검색하는 방식**이 된다.
쿠팡(`partners_link.py`)과 입력이 다르다.

```
방 메시지의 toss.im 단축링크
  → toss.shopping/t/<id> 로 해석          (검증됨)
  → og:title 에서 정확한 상품명 추출       (검증됨, '| 토스쇼핑' 접미사 제거)
  → sharelink.toss.im 에서 그 이름으로 검색  (미확인)
  → 내 쉐어링크 생성                       (미확인)
```

검색 방식이므로 **동명이인 상품을 잘못 고를 위험**이 있다. 상품 ID
(`524516537`)를 대조해 같은 상품인지 확인하는 장치가 반드시 필요하다.
엉뚱한 상품 링크를 발행하면 신뢰를 잃는다.

## 출처

- https://shopping-docs.toss.im/dev (셀러 API)
- https://sharelink-docs.toss.im/ (쉐어링크)
- https://sharelink-docs.toss.im/guide/create-sharelink.md (링크 만들기)
- https://sharelink-docs.toss.im/help/policy.md (운영 정책)
