# 작업 목록

**순서대로 진행할 것.** 각 작업은 앞 작업이 실제로 동작하는 것을 확인한 뒤 시작한다.
전부 한꺼번에 고치려 하면 어디서 깨졌는지 알 수 없게 된다.

완료한 작업은 `- [x]`로 바꾸고, 실제로 관측한 값(윈도우 클래스명, DOM 셀렉터 등)을
해당 작업 아래 `관측:` 항목으로 기록해 둘 것. 다음 사람이 추측을 반복하지 않게 하기 위함이다.

---

## T0. 환경 구축

- [x] `pip install -r requirements.txt`
- [ ] `playwright install chromium`
- [ ] `.env.example`를 참고해 환경변수 설정
- [ ] `python -c "import pywinauto, playwright, requests"` 통과

---

## T1. 카톡 내보내기 동작시키기 ✅ 완료 (2026-07-29)

`src/kakao_export.py` — **실제 환경에서 동작 확인됨.**
아래 예상 실패 지점 중 실제로 문제였던 것은 3·4번이고, 1·2번은
예상과 달랐다. 자세한 것은 이 절 끝의 `관측:` 항목들을 볼 것.

```
python src/kakao_export.py --room "방이름일부"
```

**예상 실패 지점**
1. 채팅방 창을 못 찾음 → `CLS_MAIN` / `CLS_CHAT` 상수가 실제와 다름
2. 메인 창에서 방 열기 실패 → `open_chat_from_main()`의 `Ctrl+F` 검색 흐름이 현재 카톡과 다름
3. 저장 대화상자를 못 찾음 → 대화상자 제목/구조가 다름
4. 덮어쓰기 확인 대화상자 처리 실패

**먼저 관측부터 할 것.** 고치기 전에 아래를 실행해 실제 창 정보를 확인한다.

```python
from pywinauto import Desktop
for w in Desktop(backend="win32").windows():
    try:
        print(repr(w.window_text()), w.class_name())
    except Exception:
        pass
```

**완료 기준**
- 채팅방 창이 열려 있을 때 → `export.txt` 생성 및 갱신
- 채팅방 창이 닫혀 있을 때 → 자동으로 열어서 `export.txt` 생성
- 두 번 연속 실행해도 덮어쓰기 대화상자에서 멈추지 않음

**관측:** (2026-07-28, `tools/observe_kakao.py --mode windows`)

> **창 클래스 — 실측**
>
> | 창 | class_name | title |
> |---|---|---|
> | 채팅방 | `EVA_Window_Dblclk` | `'💖 쿠팡 실시간 핫딜방 💖'` |
> | 메인 | `EVA_Window_Dblclk` | `'카카오톡'` |
>
> **메인 창과 채팅방 창이 같은 클래스다.** 구분은 제목으로만 가능하다.
> 기존 `CLS_CHAT = "#32770"` 은 틀린 값이었다. `#32770` 은 Win32 표준
> 대화상자 클래스이며, 카톡 창이 아니라 저장 대화상자가 쓰는 값이다.
>
> 다만 기존 조건이 `cls in (CLS_CHAT, CLS_MAIN)` 이라 `CLS_MAIN` 쪽에 걸려
> 창 찾기 자체는 우연히 동작하고 있었다. 예상 실패 지점 1번은 실제 실패가 아니다.
> 이름과 의미가 어긋나 오해를 부르므로 `CLS_KAKAO` / `CLS_DIALOG` 로 정리했다.
>
> **백엔드**: 두 창 모두 `win32` 와 `uia` 양쪽에서 보인다. `backend="win32"` 유지로 충분.
>
> **숨은 창 주의**: `KakaoTalk.exe` 는 제목 없는 `EVA_Window_Dblclk` 와
> `tooltips_class32` 를 수십 개(실측 43개 중 대부분) 띄워 둔다. 전부
> `visible=False` 이고 제목이 비어 있다. 제목 필터로 걸러지지만
> 숨은 창을 잡으면 `set_focus()` 가 조용히 실패하므로 visible 검사를 추가했다.
>
> **방 이름 / `--room` 문자열**
>
> 제목 코드포인트:
> `U+1F496(💖) U+0020 U+CFE0(쿠) U+D321(팡) U+0020 U+C2E4(실) U+C2DC(시) U+AC04(간) U+0020 U+D56B(핫) U+B51C(딜) U+BC29(방) U+0020 U+1F496(💖)`
>
> 이모지는 U+1F496 이고 **앞뒤로 공백이 하나씩 있다**. U+FE0F(이체자 선택자)
> 같은 보이지 않는 문자는 없다. 따라서 `--room "쿠팡 실시간 핫딜방"` 이 안전하다.
> 이모지를 포함시키면 콘솔 인코딩 문제가 생길 수 있으니 넣지 말 것.

**관측:** (2026-07-28, `tools/observe_kakao.py --mode dialog`)

> **Ctrl+S → 저장 대화상자 직행. 중간 옵션 대화상자는 없다.**
>
> 딜방 창에서 Ctrl+S 를 누르면 곧바로 아래가 뜬다.
>
> ```
> class='#32770'  title='다른 이름으로 저장'  proc=KakaoTalk.exe
> ```
>
> 제목 코드포인트는 평범한 한글이고 숨은 문자가 없다.
> 예상 실패 지점 3번("카톡이 저장 전에 옵션 대화상자를 한 번 더 띄운다")은
> **사실이 아니었다.** `kakao_export.py` 의 Ctrl+S 전제는 맞다.
>
> 대화상자가 KakaoTalk.exe 소유라는 점도 확인했다. 다른 앱의 저장
> 대화상자를 잘못 잡지 않도록 소유 프로세스로 걸러도 된다.
>
> **부속 창 주의**: Ctrl+S 한 번에 창이 17개 뜬다. 그중 실제 대화상자는
> `#32770` 하나뿐이고 나머지는 `tooltips_class32`, `Auto-Suggest Dropdown`,
> `ComboLBox`, `_SearchEditBoxFakeWindow`, `WorkerW` 같은 부속 창이다.
> 전부 제목이 비어 있어 제목으로 거르면 걸러진다.
>
> **관측 도구 자체의 버그를 여기서 발견했다.**
> `print_control_identifiers()` 는 `WindowSpecification` 의 메서드인데
> `Desktop().windows()` 와 `wrapper_object()` 는 래퍼(`DialogWrapper` 등)를
> 돌려준다. 래퍼에는 그 메서드가 없어 트리 덤프가 전부 실패했다.
> 핸들로 `WindowSpecification` 을 다시 만들도록 고쳤고, 실패 시 자식 창을
> 직접 순회하는 대체 경로를 넣었다. `--mode tree` 도 같은 원인으로 실패한다.

**관측:** (2026-07-28, `--mode dialog` 재실행 + 스크린샷)

> **Ctrl+S 이후 창 흐름 — 3단계다.** 기존 코드는 2단계까지만 알고 있었다.
>
> ```
> '다른 이름으로 저장'      #32770   → 파일명 입력 → 저장(&S)
> '다른 이름으로 저장 확인'  #32770   → 예(&Y)          (파일이 이미 있을 때만)
> '대화 내보내기'           (미관측)  → 확인            ← 기존 코드에 없던 단계
> ```
>
> **컨트롤 (실측)**
>
> | 창 | 제목 | 컨트롤 |
> |---|---|---|
> | 저장 | `다른 이름으로 저장` | `Edit`(파일명), `저장(&S)`, `취소`, `도움말(&H)`, `ComboBox`='Text Files (*.txt)' |
> | 덮어쓰기 | `다른 이름으로 저장 확인` | `예(&Y)`, `아니요(&N)` |
> | 완료 | `대화 내보내기` | `확인`, `폴더열기` |
>
> **⚠️ 제목 부분일치 함정 — 실제 버그였다**
>
> `'다른 이름으로 저장 확인'` 은 `'다른 이름으로 저장'` 을 부분문자열로
> 포함하고 **둘 다 `#32770`** 이다. 기존 코드의
> `any(k in t for k in ("다른 이름으로 저장", "Save As", "저장"))` 는
> 덮어쓰기 확인창을 저장 대화상자로 오인할 수 있었다.
> 제목 매칭을 정확일치로 바꿨다. 부분일치로 되돌리지 말 것.
>
> 기존의 덮어쓰기 처리도 `"확인" in t` 인 아무 창에나 Alt+Y 를 6번
> 반복해 보내고 있었다. "확인"은 흔한 단어라 무관한 창을 건드릴 수 있다.
> 정확일치 + 버튼 클릭으로 교체했다.
>
> **완료 알림창을 반드시 닫아야 한다**
>
> 저장이 끝나면 카톡이 '대화 내보내기 / 완료되었습니다' 알림을 띄우고
> 사용자가 확인을 눌러야 사라진다. 기존 코드에는 이 단계가 아예 없어서
> 창이 남고 다음 주기의 Ctrl+S 가 막힌다.
> 클래스명은 아직 미관측(스크린샷으로만 확인)이라 제목으로만 찾고,
> 찾으면 실제 클래스명을 로그에 남기도록 했다. **다음 실행 로그에서
> 그 값을 확인해 여기에 기록할 것.**
>
> **파일명 입력 방식 변경**
>
> `Alt+N` → `send_keys(경로)` 대신 `Edit` 컨트롤에 `set_edit_text()` 로
> 직접 넣는다. 실제 `Edit` 컨트롤이 존재함을 확인했다. 키 입력이 아니라
> 경로의 공백·특수문자`(){}+^~%`에 안전하고 IME 상태에도 영향받지 않는다.

**관측:** (2026-07-29, 실제 실행 — 스크립트로 `export.txt` 생성 성공)

> **`kakao_export.py` 가 실제로 동작한다.** 새 `export.txt` 가 생성됐다
> (`저장한 날짜 : 2026-07-29 00:10:03`, 6,500줄 265KB).
> 창 찾기 → Ctrl+S → 경로 입력 → 저장 → 덮어쓰기 확인까지 전부 통과했다.
>
> **남은 문제: 완료 알림창이 자동으로 안 닫힌다.**
>
> 원인 두 가지를 소유자가 짚어 주었다.
>
> 1. **알림창 자체에는 `set_focus()` 가 먹지 않는다.**
>    **카톡 채팅방 창에 포커스를 줘야** Enter 가 알림창까지 전달된다.
>    → `close_done_dialog(chat_win=...)` 로 채팅방 창을 넘겨 거기에
>      포커스를 주도록 고쳤다.
> 2. **파일이 다 쓰인 뒤에도 몇 초 더 기다려야 한다.**
>    게이지가 100% 로 바뀌고 버튼이 살아나는 데 시간이 걸린다.
>    → `SETTLE_AFTER_SAVE = 5.0` 추가.
>
> 닫혔는지 `is_dialog_open()` 으로 확인하고 최대 3회 재시도한다.
> 창이 남으면 다음 주기의 Ctrl+S 가 막히므로 조용히 넘어가면 안 된다.
>
> 이 알림창은 진행률 0% 일 때 이미 떠 있다. 창의 존재만으로 완료를
> 판단하지 말 것. `wait_file_ready()` 로 실제 파일이 쓰였는지를 본다.

**관측:** (2026-07-29, 완료 알림의 정체 확정)

> **완료 알림은 Win32 객체가 아니다. 카톡이 직접 그린 것이다.**
>
> 최상위 창 목록에도 없고, 채팅방 창의 자식 컨트롤로도 안 잡힌다.
> 채팅방 창의 자식은 이게 전부다.
>
> ```
> RICHEDIT50W               '관리자만 말할 수 있는 상태입니다.'
> EVA_VH_ListControl_Dblclk (메시지 목록)
> _EVA_CustomScrollCtrl     (스크롤바)
> Edit                      (크기 0)
> ```
>
> EVA 캔버스에 직접 렌더링된 모달이라 **Win32 API 로는 존재 자체를
> 감지할 수 없다.** 따라서 다음이 성립한다.
>
> - 창을 찾아서 버튼을 클릭하는 방식은 **원리적으로 불가능**하다
> - 닫혔는지 여부도 **확인할 수 없다**. 코드가 확인한 척하면 안 된다
> - 상호작용 수단은 키보드 입력 또는 좌표 클릭뿐이다
>
> **해법(검증됨): 채팅방 창에 포커스를 주고 Enter.**
> 알림창 자체에는 `set_focus()` 가 먹지 않는다. 카톡 창을 활성화해야
> Enter 가 모달까지 전달된다.
>
> 좌표 클릭은 넣지 않았다. Enter 로 해결되므로 화면 구성 변화에
> 취약한 좌표에 의존할 이유가 없다. 필요해지면 스크린샷 기준
> 채팅방 창의 가로 55% · 세로 59% 지점이 '확인' 버튼이다.

**완료 기준 — 전부 충족 ✅**
- [x] 채팅방 창이 열려 있을 때 → `export.txt` 생성 및 갱신
- [x] 두 번 연속 실행해도 덮어쓰기 대화상자에서 멈추지 않음
- [x] 완료 알림창 자동 닫기
- [ ] 채팅방 창이 닫혀 있을 때 자동으로 열기
      — **미검증.** 방을 열어두고 운영하면 이 경로를 타지 않는다.
      `run_all.py` 무인 운영(T5) 전에 확인할 것. 카톡 재시작 후
      방 창이 닫힌 상태로 시작되면 여기서 막힌다.

**미관측 (남은 것):**
- `대화 내보내기` 완료 알림창의 클래스명 (다음 실행 로그에서 확인)
- 메인 창의 컨트롤 트리 — `Ctrl+F` 검색 흐름 (예상 실패 지점 2).
  단 방 창을 열어둔 채 운영하면 이 경로를 안 타므로 우선순위는 낮다.

---

### 내보낸 파일 실측 (2026-07-28)

| 항목 | 값 |
|---|---|
| 인코딩 | **UTF-8 (BOM 없음)** — `kakao_deal_extract.py` 의 `encoding="utf-8"` 그대로 OK |
| 크기 | 6,500줄 / 265KB |
| 헤더 | `💖 쿠팡 실시간 핫딜방 💖 님과 카카오톡 대화` + `저장한 날짜 : ...` |
| 메시지 줄 | `[실시간 핫딜 방장] [오전 10:10] 본문` → `RE_PC_LINE` 일치 |
| 날짜 구분선 | `--------------- 2026년 6월 21일 일요일 ---------------` → `RE_DATE_SEP` 일치 |

기존 정규식이 실제 포맷과 맞는다. T2 의 파싱 수정은 불필요하다.

---

## T2. 파싱 정확도 맞추기

`src/kakao_deal_extract.py` — 샘플로는 동작하나 실제 방 포맷은 미확인.

```
python src/kakao_deal_extract.py export.txt --no-resolve
```

**할 일**
- 실제 `export.txt`로 돌려 `pending.csv`의 `title`이 상품명으로 제대로 잡히는지 확인
- 안 맞으면 `guess_title()`의 필터 규칙을 실제 메시지 포맷에 맞게 조정
- 가격이 여러 개 나올 때 `guess_price()`가 최솟값을 고르는데, 그 방에서 이게 맞는지 확인
  (원가와 할인가를 같이 쓰는 방이면 맞고, 아니면 로직 변경 필요)

**완료 기준**
- 실제 딜 메시지 10건 중 8건 이상에서 상품명과 가격이 정확히 추출됨
- 딜이 아닌 잡담 메시지는 하나도 잡히지 않음 (오탐 0)

**그다음 네트워크 해석 테스트** — `--no-resolve` 없이 실행

- 쿠팡이 봇 트래픽을 차단하는지 확인. `productId 확정 실패`가 반복되면
  `resolve_product_url()`을 `partners_link.py`의 Playwright 컨텍스트를 재사용하도록 변경
- 실패한 URL은 다음 실행에서 자동 재시도되게 이미 되어 있다. 이 동작을 깨지 말 것.

**관측:** (2026-07-28, 실제 `export.txt` 6,500줄로 파서 직접 실행)

> **링크 구성** — 이 방은 쿠팡과 토스쇼핑을 섞어 올린다.
>
> | 링크 | 총 건수 | 고유 |
> |---|---|---|
> | `link.coupang.com` | 296 | **264** |
> | `toss.im/_m` | 232 | 115 |
> | `coupa.ng`, `coupang.com/vp` | 0 | 0 |
>
> 고유 기준으로 쿠팡이 2배 이상 많다. 현재 `RE_COUPANG` 이 264건을 잡는다.
> 쿠팡 링크는 **전부 `link.coupang.com` 단축링크**이므로 `--no-resolve` 로는
> `productId` 를 한 건도 못 얻는다. 네트워크 해석이 필수다.
>
> **추출 품질 — 완료 기준 충족**
>
> | 항목 | 기준 | 실측 |
> |---|---|---|
> | 상품명·가격 정확 추출 | 10건 중 8건 이상 | **10/10** |
> | 잡담 오탐 | 0건 | **0건** |
>
> 메시지 815건 중 쿠팡 링크 포함 268건, 나머지 547건(공지·잡담)은
> 링크가 없어 전부 걸러진다. `guess_title()` 은 상품명을 정확히 뽑는다.
>
> **🔴 `guess_price()` 가 틀린 값을 고른다 — 수정 필요**
>
> 이 방의 가격 표기는 이렇다.
>
> ```
> 🚨 역대 최저가 2,980원 🚨          ← 실제 판매가
> ↳ 평균가 대비 🔻 2,439원 🔻 46%    ← 할인 '금액'
> ```
>
> `guess_price()` 는 최솟값을 고르므로 **할인액 2,439원을 가격으로 쓴다.**
> 실제 가격은 2,980원이다. 다른 건도 같다 (20,100원 → 7,187원으로 오인).
>
> TASKS.md 원문은 "원가와 할인가를 같이 쓰는 방이면 최솟값이 맞다" 고
> 했는데, 이 방은 **최저가 + 할인액** 을 쓴다. 최솟값 규칙이 안 맞는다.
> 이대로 두면 모든 게시물의 가격이 틀리게 나간다.
>
> → `역대 최저가 N원` 패턴을 우선 매칭하도록 고칠 것.
>   그 패턴이 없을 때만 기존 방식으로 넘어간다.

**관측:** (2026-07-29, 수정 후 268건 전체 재검증)

> **쿠팡 봇 차단 — 403 을 주지만 해석은 된다**
>
> ```
> HTTP 403  리다이렉트 1회
> 최종: https://www.coupang.com/vp/products/2279371037?itemId=...&src=1139000&spec=10
> productId: 2279371037  ✅
> ```
>
> 상태코드는 403 이지만 리다이렉트를 따라가고 최종 URL 에 productId 가
> 들어 있다. `resolve_product_url()` 은 `r.url` 에서 뽑으므로 지장이 없다.
> **상태코드로 실패 판정을 하지 말 것.**
> 다만 쿠팡이 나중에 리다이렉트까지 막으면 여기가 조용히 실패한다.
> `productId 확정 실패` 가 반복되면 이걸 의심할 것.
>
> **🔴 방장의 트래킹 파라미터가 딸려 온다 — 제거함**
>
> 최종 URL 에 `src=1139000` 이 붙어 있다. 방장의 유입 소스다.
> 그대로 `partners_link.py` 에 넘기면 내 링크를 만드는 입력에 남의
> 트래킹이 섞인다. `clean_product_url()` 로 제거한다.
> `itemId` / `vendorItemId` 는 **남긴다.** 옵션(색상·용량)을 가리키므로
> 빼면 엉뚱한 옵션의 링크가 만들어진다.
>
> **가격 로직 수정 결과 (268건 전체)**
>
> | 항목 | 결과 |
> |---|---|
> | `최저가 N원` 패턴 매칭 | 235/268 (87.7%) |
> | **가격이 교정된 건** | **176건** |
> | 가격 없음 | 31건 — 전부 카테고리·모음 메시지(단일 상품 아님) |
> | 회귀 | **0건** |
>
> 268건 중 176건, 즉 **3분의 2가 틀린 가격으로 나갈 뻔했다.**
>
> `원` 을 빠뜨린 표기가 실제로 있다 (`역대 최저가 15,420`).
> `최저가` 가 앵커 역할을 하므로 `원` 은 선택으로 뒀다.
>
> **가격 없는 31건은 정상이다.** "와우 멤버스데이 할인 모음 전체 보기"
> 같은 카테고리 링크 메시지다. 이들은 `/vp/products/` 로 리다이렉트되지
> 않아 `productId 확정 실패` 로 걸러지므로 `deals` 에 들어가지 않는다.
>
> **상품명**: 앞머리 이모지(`✅`) 제거. 268건 전부 추출 성공,
> 기호로 시작하는 제목 0건.

---

## T3-a. 쿠팡 파트너스 링크 생성 ✅ 완료 (2026-07-29)

`src/partners_link.py` — **실제 환경에서 딥링크 생성 성공.**
단 **폐기 예정 코드이므로 최소한으로만 손댈 것.**

```
py src/partners_link.py --login      # 최초 1회, 수동 로그인
py src/partners_link.py --limit 1
```

**완료 기준 — 전부 충족 ✅**
- [x] 상품 URL 1건 → 딥링크 생성 및 `deals.affiliate_url` 저장
- [x] 생성된 링크가 정상 상품 페이지로 이동
- [x] 트래킹 검증 — `lptag` 가 붙어 있다

실측 1건:

```
대상 종가 총각김치, 2.3kg, 1개   17,330원
  상품 5140279812 → https://link.coupang.com/a/fLz3Rghg5s
  링크 열기 → coupang.com/vp/products/5140279812?...&lptag=AF4612286&...
  HTTP 403 이지만 리다이렉트 1회로 정상 도달 (T2 관측과 같은 현상)
```

자가탐색 없이 8초. 파트너스 관리자 페이지 조회는 아직 안 해 봤지만,
`lptag=AF4612286` 이 내 파트너스 ID 이므로 트래킹은 붙었다.

**관측:** (2026-07-29, `tools/observe_coupang.py`)

> **🔴 보고 있던 페이지가 애초에 틀렸다**
>
> `LINK_PAGE` 가 `#affiliate/ws/link` 였는데 그건 **'상품 링크'** 화면이다.
> 상품을 검색해서 고르는 3단계 마법사다.
>
> ```
> 1 상품 탐색 → 2 마음에 드는 상품 선택 → 3 URL 혹은 배너 만들기
> ```
>
> 입력창이 `찾고 싶은 상품을 검색해보세요!` **검색창 하나뿐**이고
> 링크 생성 버튼 자체가 없다. URL 을 붙여넣는 전제와 맞지 않는다.
> 자가탐색을 돌렸으면 검색창에 상품 URL 을 넣고 Enter 를 쳤을 것이다.
>
> URL 을 붙여넣는 화면은 **'간편 링크 만들기'** 다.
>
> ```
> #affiliate/ws/link-to-any-page   ← 이것
> ```
>
> **상단 '링크 생성' 드롭다운은 hover 해야 DOM 에 나타난다.** 그래서
> 정적 덤프에서는 하위 메뉴가 하나도 안 보였다. hover 로 드러난 전체:
>
> | 메뉴 | 경로 |
> |---|---|
> | 간편 링크 만들기 | `#affiliate/ws/link-to-any-page` |
> | 상품 링크 | `#affiliate/ws/link` |
> | 검색 위젯 | `#affiliate/ws/search-bar` |
> | 이벤트/프로모션 링크 | `#affiliate/ws/events` |
> | 카테고리 배너 | `#affiliate/ws/banner` |
> | 다이나믹 배너 | `#affiliate/ws/dynamic-widgets` |
> | **파트너스 API** | `#affiliate/ws/tools/open-api` ← T6 에서 쓴다 |
>
> **간편 링크 만들기 화면 (실측)**
>
> | 항목 | 값 |
> |---|---|
> | 입력창 | `<input type=text id="url">` 폭 1012 |
> | 버튼 | `링크 생성` + **U+200B(폭 0 공백)** |
> | iframe | **0개** — iframe 걱정은 기우였다 |
>
> ⚠️ 버튼 텍스트 끝에 보이지 않는 U+200B 이 붙어 있다. 정확일치로 찾으면
> 못 찾는다. `attempt()` 가 `exact=False` 부분일치를 쓰므로 상수에는
> 폭 0 공백 없이 적었다.
>
> 이 값들을 `KNOWN_SELECTORS` 에 넣어 자가탐색보다 먼저 쓰게 했다.
> 실제 화면을 봐서 아는 값이 있는데 탐색부터 돌릴 이유가 없다.
> 탐색은 시도 한 번이 곧 사이트 접근 한 번이다.

**관측:** (2026-07-29, `tools/observe_coupang.py --issue` — 생성 계측)

> **✅ 링크 생성 API 를 찾았다**
>
> ```
> GET https://partners.coupang.com/api/v1/url/any?coupangUrl=<상품URL>
> {"rCode":"0","rMessage":"","data":{
>    "type":"sdp",
>    "shortUrl":"https://link.coupang.com/a/fLzMJE46fI",
>    "landingUrl":"https://link.coupang.com/re/AFFSDP?lptag=AF4612286
>                  &pageKey=5140279812&itemId=...&vendorItemId=...&traceid=...",
>    "title":"쿠팡을 추천 합니다!",
>    "description":"[로켓프레시] 대상 종가 총각김치"}}
> ```
>
> `landingUrl` 의 **`pageKey` 가 상품 ID** 다. 이걸로 **받은 링크가 정말
> 그 상품의 것인지 대조한다.** 화면 긁기는 이 대조를 못 한다. 페이지에
> 남아 있던 이전 링크를 주워도 알 방법이 없고, 엉뚱한 상품의 링크를
> 발행하면 신뢰를 잃는다. `expect_pid` 와 다르면 링크를 버린다.
>
> `lptag` 는 내 파트너스 ID 다. 이게 없으면 클릭은 되지만 수익이 0으로
> 찍히므로(제약 4) 없으면 역시 버린다.
>
> **쿠팡은 토스와 달리 화면에도 링크를 보여준다.** `파트너스 URL` 칸에
> 나오고 `JS_HARVEST` 가 1건 잡는다. 그래서 API 를 놓쳐도 대비책이
> 유효하다. 다만 그 경로에서는 상품 대조를 할 수 없다.
>
> **🔴 고지 문구가 틀려 있었다 — 수정함**
>
> 이 화면이 필수 문구를 직접 명시한다.
>
> ```
> 1. 게시글 작성 시, 아래 문구를 반드시 기재해 주세요.
> "이 포스팅은 쿠팡 파트너스 활동의 일환으로, 이에 따른 일정액의
>  수수료를 제공받습니다."
> 쿠팡 파트너스의 활동은 공정거래위원회의 심사지침에 따라 ...
> ```
>
> `threads_post.py` 의 `DISCLOSURE` 는 **`이 게시물은`** 으로 시작하고
> 있었다. 쿠팡이 지정한 것은 **`이 포스팅은`** 이다. 공정위 심사지침이
> 걸린 문구라 임의로 바꾸면 안 된다. 상수를 쿠팡 문구로 맞췄다.
> (토스 문구는 원래 맞았다.)

**관측:** (2026-07-29, 로그인 세션이 안 남던 문제 — `tools/observe_coupang_session.py`)

> **'자동 로그인' 을 켜야 세션이 남는다.**
>
> 처음 두 번은 로그인에 성공했는데도(스크린샷이 완전히 로그인된 화면)
> 브라우저를 껐다 켜면 로그아웃이었다. 프로필의 쿠키를 봤더니 로그인
> 전후 집합이 **완전히 동일**했다. 남은 건 전부 추적용이다.
>
> ```
> PCID, MARKETID, _ga, sid, AFATK, Akamai bm_* / _abck / ak_bmsc
> ```
>
> 회원 인증 쿠키가 아예 없었다. '자동 로그인'(`#login-keep-state`)을
> 켜고 로그인하니 그제서야 나타났다. **전부 영속(1년)** 이다.
>
> ```
> CT_AT, CSID, CUPT, CPUSR_RL, member_srl, ILOGIN, rememberme
> ```
>
> 인증은 전부 쿠키가 들고 있다. localStorage 에는 Akamai 값
> (`ak_a`, `ak_ax`, `check_feat_list`)뿐이고 sessionStorage 에는
> `ak_bm_tab_id` 뿐이다. `storage_state` 로 복원해도 로그인이 유지된다.
>
> 해시 라우트 직접 진입이 원인인가도 확인했는데 루트부터 로그아웃이었다.
> 라우팅 문제가 아니었다.
>
> → `do_login()` 이 '자동 로그인' 을 직접 켜고, **로그인 후 브라우저를
>   껐다 켜서 세션이 실제로 남는지 검증**한다. 로그인 직후에 로그인
>   상태인 건 당연하다. 문제는 디스크에 남느냐다.
>
> → `is_logged_in()` 추가. URL 로만 판정하면 안 된다. 해시 라우트라
>   로그아웃 상태에서도 같은 주소에 머무를 수 있다. `do_run()` 의
>   세션 만료 판정도 `"login" in page.url` 에서 이걸로 바꿨다.

**관측:** (2026-07-29, 로그인 화면 — 참고용)

> **프레임 구성 (로그인 화면 기준)**
>
> | name | url |
> |---|---|
> | (main) | `login.coupang.com/login/login.pang?rtnUrl=...` |
> | `pc-otp-login-iframe` | `about:blank` |
> | (익명) | `www.coupang.com/dpop/iframe` |
>
> **OTP 로그인 iframe 이 있다.** 로그인에 추가 인증이 붙을 수 있다는
> 뜻이고, 세션이 만료되면 무인 운영이 거기서 멈춘다. T5 전에
> 세션이 얼마나 오래 살아 있는지 확인해야 한다.
>
> iframe 걱정도 했었는데 로그인 후 링크 생성 화면에는 iframe 이 0개다.
> 기우였다. (관측 도구는 프레임별 입력창 개수를 계속 세 준다.)

**🔴 자가탐색이 제약 2 를 어기고 있었다 — 수정함 (2026-07-29)**

> `discover()` 는 입력창 5개 × 버튼 4개 = **최대 20회**를 **대기 없이
> 연속으로** 시도했다. 한 번 시도할 때마다 페이지를 새로 열고
> (`goto_link_page`) 폼을 채워 제출한다. 즉 **시도 횟수가 곧 파트너스
> 사이트 접근 횟수**다. 20번을 쉬지 않고 두드리는 건 사람의 행동이 아니고
> CLAUDE.md 제약 2 를 정면으로 어긴다.
>
> - `MAX_DISCOVER_ATTEMPTS = 8` 로 상한을 두고 시도 사이에 6초를 넣었다
> - `MAX_LINKS_PER_RUN = 4` 를 `partners_link.py` 자체에도 넣었다.
>   `run_all.py` 는 `--limit 4` 로 넘겨주지만 직접 실행하면 기본이
>   10건이었다. `toss_link.py` 는 이미 자기 상한을 강제하고 있었다.
>
> **이 값들을 올리지 말 것.**
>
> 지금은 `KNOWN_SELECTORS` 가 있어서 정상 경로에서는 탐색이 아예 안 돈다.
> 탐색은 쿠팡이 화면을 바꿨을 때만 대비책으로 돈다.

**남은 것**
- [ ] 파트너스 관리자 페이지에서 그 링크가 내 계정 링크로 조회되는지
      (`lptag` 확인으로 갈음했지만 관리자 화면 대조는 아직)
- [ ] 세션 수명. 인증 쿠키는 1년짜리지만 실제로 얼마나 버티는지는 모른다.
      로그인 화면에 OTP iframe 이 있어 재로그인에 추가 인증이 붙을 수 있다.
      만료되면 무인 운영이 거기서 멈춘다. T5 전에 확인할 것.

---

## T2 정정 — `src=1139000` 은 방장 것이 아니다 (2026-07-29)

앞선 T2 관측은 최종 URL 의 `src=1139000` 을 "방장의 유입 소스" 로
단정했다. **틀렸다.**

`clean_product_url()` 로 `src` 를 제거한 URL 을 넣어 만든 **내 딥링크**의
최종 URL 에도 `src=1139000` 이 그대로 붙는다.

```
입력  : coupang.com/vp/products/5140279812?itemId=...&vendorItemId=...
        (src 없음)
내 링크: link.coupang.com/a/fLz3Rghg5s
최종  : coupang.com/vp/products/5140279812?...&src=1139000&...&lptag=AF4612286&...
```

즉 `src` 는 쿠팡이 파트너스 링크에 공통으로 붙이는 소스 코드이고,
파트너를 식별하는 것은 **`lptag`** 다. 제거 자체는 무해하므로
`clean_product_url()` 은 그대로 둔다. 다만 "방장 트래킹을 떼어냈다" 는
설명이 틀렸으므로 여기 적어 둔다.

**다음에 할 것 (로그인 이후)**

```
py src/partners_link.py --login          # 사람이 직접
py tools/observe_coupang.py              # 읽기 전용. 먼저 본다
py tools/observe_coupang.py --issue "https://www.coupang.com/vp/products/..."
```

토스에서 배운 것을 먼저 확인한다. 토스는 발급된 링크를 DOM 에 전혀
넣지 않아 화면 긁기가 원리적으로 실패했고, 발급 API 응답을 가로채니
한 번에 풀렸다(T3-b 참고). 쿠팡도 같은 구조일 수 있으므로
`--issue` 모드가 네트워크까지 기록한다. 자가탐색을 돌리기 전에 이걸 본다.

---

## T3-b. 토스 쉐어링크 발급 ✅ 완료 (2026-07-29)

`src/toss_link.py` — **실제 환경에서 발급 성공.**

```
py src/toss_link.py --login      # 최초 1회. 토스 비즈니스 계정
py src/toss_link.py --dry-run    # 발급 없이 대상 목록만
py src/toss_link.py --limit 1    # 실제 발급
```

**완료 기준 — 전부 충족 ✅**
- [x] 상품 카드가 스캔된다 (118개)
- [x] 상품명·가격·개당 수익·특가율·평점이 읽힌다
- [x] `링크 발급` 클릭 후 `toss.im/_m/...` 링크를 얻는다
- [x] 상품 ID 가 확정되고 `deals.affiliate_url` 에 저장된다
- [x] 발급된 링크를 열면 그 상품 페이지로 간다 (트래킹 검증)

실측 1건:

```
프리미엄 엠머 파로 효소, 3g, 14포, 4박스   9,900원  92% 특가  30일최저
  상품ID 162535755 → https://toss.im/_m/Hxt0f2Gi
  링크 열기 → https://toss.shopping/t/162535755?k=1d56ffaf-...&referrer=affiliate
```

**관측:** (2026-07-29, `tools/observe_toss_cards.py`, 프로모션 상품 118개)

> **`PRODUCTS_URL` 은 실재한다.** 이전 세션에 관측된 것은 `/home` 뿐이었고
> `https://sharelink.toss.im/links/recommended-products` 는 추측값이었는데,
> 열어 보니 실제 '프로모션 상품' 화면이다. 카테고리·특가·기획전 필터가
> 있고 `링크 발급` 버튼이 118개 있다.
>
> 참고로 **`/links` 는 없다.** `/home` 으로 튕긴다. 사이드바의 '링크' 는
> 드롭다운이고 하위가 '베스트 랭킹' / '상품 조회' 다.
> **내가 발급한 링크의 목록 화면은 아직 못 찾았다.**
>
> **✅ 인덱스 정합성 — 118/118 일치**
>
> `issue_link()` 는 `get_by_role("button", name="링크 발급").nth(idx)` 로
> 누르고 그 `idx` 는 `JS_SCAN_CARDS` 가 매긴 번호다. 두 목록의 순서가
> 어긋나면 **엉뚱한 상품의 링크를 조용히 발급한다.** 버튼에
> `data-pm-idx` 를 찍어 전부 대조했고 하나도 어긋나지 않았다.
> Playwright 개수 118 = JS 개수 118.
>
> **🔴 카드 경계가 한 단계 얕아 특가율을 놓치고 있었다**
>
> 기존 규칙은 "innerText 길이 30 초과인 첫 조상" 인데 그러면 `up=2` 에서
> 멈춘다. `68% 특가` 배지는 `up=3` 에서 들어온다. `salePct` 가
> **118/118 전부 None** 이었다. 조상 단계별 실측:
>
> | 단계 | 특가 | 발급버튼 | len |
> |---|---|---|---|
> | up1 | 0 | 1 | 5 |
> | up2 | 0 | 1 | 89 |
> | up3 | **1** | **1** | 96 |
> | up4 | 1 | 1 | 96 |
> | up5 | 118 | 118 | 10467 ← 격자 전체로 터진다 |
>
> `up4` 와 `up5` 사이에서 뚜렷하게 갈리므로 모호함이 없다.
> **'링크 발급' 을 정확히 1개만 품는 가장 바깥 조상**을 카드로 보도록
> 바꿨다. 스스로 검증되는 규칙이라 옆 카드가 섞일 수 없다.
>
> **🔴 개당 수익 정렬 = 가격 정렬이었다**
>
> 수수료는 가격의 **정확히 10%** 다. 118건 전부 9.99~10.00% 안에 들어온다.
> 따라서 `개당 수익` 순 정렬은 아무 정보도 주지 않는다. 실제 결과:
>
> ```
> 1,187,010원  SK매직 정수기      개당 118,701원  [30일최저]
>   670,000원  현대큐밍 냉장고     개당  67,000원  [30일최저]
>   595,000원  클라쎄 김치냉장고   개당  59,500원  [30일최저]
> ```
>
> 딜방 가격 분포는 이렇다. **중앙값 24,800원.**
>
> | 가격대 | 건수 |
> |---|---|
> | ~1만원 | 11 |
> | 1~3만원 | **62** |
> | 3~10만원 | 21 |
> | 10~50만원 | 17 |
> | 50만원~ | 7 |
>
> 고가 가전만 골라 올릴 뻔했다. `30일최저 > 특가율 > 리뷰수` 로 바꿨다.
> **이 순서는 잠정값이다. 무엇을 올릴지는 사업 판단이라 소유자 확인이
> 필요하다.** `--max-price` 로 상한을 걸 수 있게 해 뒀다(기본 없음).
>
> **🔴 카드에 상품 ID 가 없다 — 발급 전 중복 제거 불가**
>
> 카드 안의 `href` / `img src` / `data-*` 를 전부 훑었지만 상품 ID 가
> 없다. 이미지 URL 은 `resources-fe.toss.im/.../<UUID>.jpeg` 형태라
> 상품과 무관하다(6자리 이상 숫자가 44/118 에서 잡히지만 전부 UUID
> 오탐이다). 상품 ID 는 **링크를 발급해 봐야만** 알 수 있다.
>
> 그래서 제목으로 먼저 거른다. 제목이 우연히 겹치면 그 건을 건너뛸 뿐
> 잘못된 링크를 만들지는 않으므로 안전한 방향의 오차다.
> 최종 판정은 발급 후 `already_known()` 이 상품 ID 로 다시 한다.

**관측:** (2026-07-29, `tools/observe_toss_issue.py` — 클릭 순간 계측)

> **🔴 발급된 링크는 DOM 에 아예 나타나지 않는다.**
>
> 기존 `issue_link()` 는 클릭 후 화면 텍스트와 input value 에서
> `toss.im/_m/...` 를 찾았다. 그런데 화면에 뜨는 건 이것뿐이다.
>
> ```
> +0.4s  링크를 복사했어요.
> +0.4s  쉐어링크 발급 경험에 대해 평가해주세요.
> ```
>
> 링크는 **클립보드로 간다.** 즉 화면 긁기는 **원리적으로 성공할 수
> 없었다.** 대기시간을 늘리거나 셀렉터를 고쳐도 안 되는 종류의 문제다.
> 처음 `--limit 1` 이 실패한 원인이 이것이다.
>
> **✅ 발급 API 를 찾았다 — 이게 정답 경로다**
>
> ```
> POST https://sharelink.toss.im/api-public/v3/shopping/sharelink/link/issue
> {"resultType":"SUCCESS","success":{
>    "shortUrl":"https://toss.im/_m/vloN92Cn",
>    "originUrl":"https://toss.shopping/t/599038939?k=...&referrer=affiliate",
>    "affTrackKey":null}}
> ```
>
> `originUrl` 에 **상품 ID 가 들어 있다.** 그래서 발급된 링크를 새 탭으로
> 따라가 상품 ID 를 확인하던 `resolve_product_id()` 단계가 통째로 없어졌다.
> 사이트 접근이 한 번 줄고 실패 지점도 하나 사라진다. 발급 한 건이
> 20초 대기에서 **1초**로 줄었다.
>
> 버전이 바뀔 수 있어 `/sharelink/link/issue` 부분만으로 매칭한다.
>
> ⚠️ **이건 링크를 손으로 조립하는 게 아니다**(CLAUDE.md 제약 4).
> 토스 UI 의 발급 버튼을 그대로 누르고, 토스가 돌려준 링크를 읽을 뿐이다.
>
> 대비책으로 클립보드 읽기를 남겼다. 그 경로로 오면 상품 ID 가 없으므로
> `resolve_product_id()` 가 다시 대비책이 된다. 둘 다 실패하면 발급된
> 링크와 상품명을 로그에 남긴다. **발급만 하고 기록이 없는 상태로
> 조용히 넘어가면 안 된다.**
>
> **✅ 고지 문구가 우리 상수와 글자 단위로 같다**
>
> 토스가 클립보드에 넣어주는 내용:
>
> ```
> ✱ 이 포스팅은 토스쇼핑 쉐어링크 활동의 일환으로, 이에 따른 일정액의 수수료를 제공받습니다.
> 똑순이 알고니 알탕, 700g, 2개
> https://toss.im/_m/vloN92Cn
> ```
>
> `threads_post.py` 의 `TOSS_DISCLOSURE` 와 정확히 일치한다.
> 앞의 `✱ ` 는 토스 UI 의 장식이다. **우리 상수가 맞다는 확인이므로
> 그대로 둔다.** `build_text()` 의 `assert body.startswith(disclosure)` 가
> 있으므로 `✱ ` 를 붙이면 오히려 깨진다.
>
> 텔레메트리도 보인다: `product_link_copied`,
> `surface: recommended_products`, `positionInPage: 0`.
> 인덱스가 맞다는 또 하나의 방증이다.

**남은 것**
- [ ] 선별 기준 확정 — 소유자 판단 필요 (지금은 30일최저 > 특가율 > 리뷰수)
- [ ] 내가 발급한 링크의 목록 화면을 못 찾았다.
      계측 과정에서 발급한 2건(`똑순이 알고니 알탕`,
      최초 실패한 `--limit 1` 의 idx 21 건)은 DB 에 없다.
      회수하려면 그 화면이 필요하다. 무해하므로 급하지 않다.
- [ ] 수수료 10% 는 **9월 25일까지** 프로모션이다. 영구 요율이 아니다.

---

## T4. Threads 인증 및 발행

`src/threads_post.py` — API 호출 경로 전체 미검증.

**할 일**
1. Meta 개발자 콘솔에서 앱 생성 → Threads API 제품 추가
2. 본인 Threads 계정을 테스터로 등록하고 초대 수락
3. 리디렉션 URI 등록, `threads_basic` + `threads_content_publish` 권한
4. OAuth 코드 → 단기 토큰 → 장기 토큰(60일) 교환
5. `GET /me?fields=id`로 `user_id` 조회
6. `token.json` 생성 (`.gitignore`에 있으니 커밋되지 않는다)

**만들 것**: `src/threads_auth.py` — ✅ **작성 완료 (2026-07-29)**

```
py src/threads_auth.py            # 인증 URL 출력 → 붙여넣기 → token.json 저장
py src/threads_auth.py --check    # 지금 토큰이 살아 있는지만 확인
```

**관측:** (2026-07-29, 공식 문서로 엔드포인트 확인)

> 추측으로 쓰면 소유자가 Meta 콘솔에서 헛수고를 하게 되므로 문서로 확인했다.
> 출처: `developers.facebook.com/docs/threads/get-started/get-access-tokens-and-permissions`
>
> | 단계 | 메서드 | 주소 |
> |---|---|---|
> | 승인 | 브라우저 | `https://threads.net/oauth/authorize` |
> | 코드 → 단기(1시간) | POST | `https://graph.threads.net/oauth/access_token` |
> | 단기 → 장기(60일) | GET | `https://graph.threads.net/access_token` `grant_type=th_exchange_token` |
> | 갱신 | GET | `https://graph.threads.net/refresh_access_token` `grant_type=th_refresh_token` |
>
> 갱신 엔드포인트는 `threads_post.py` 가 이미 쓰던 것과 일치한다.
>
> **⚠️ 함정: 인증 코드 끝에 `#_` 가 붙어서 돌아온다.** 공식 문서가 명시한
> 동작이고 코드의 일부가 아니다. 안 떼면 교환이 실패한다.
> `extract_code()` 가 주소 통째로 붙여넣기 / 코드만 붙여넣기 / 따옴표 포함 /
> 승인 거부(`error=`) 를 모두 처리한다. 단위 시험으로 확인했다.
>
> 리디렉션 URI 는 **로컬 서버를 띄우지 않는다.** 승인 후 주소창의 URL 을
> 복사해 붙여넣는 방식이라 실제로 열리지 않는 주소여도 된다.
> 다만 콘솔에 등록한 값과 **끝의 슬래시까지** 정확히 같아야 한다.

**🔴 `.env` 를 읽는 코드가 아무 데도 없었다 — 수정함 (2026-07-29)**

> `.env.example` 은 "복사해서 `.env` 로 쓰거나 시스템 환경변수로 설정" 이라고
> 안내하는데, 실제로 `.env` 를 읽는 코드가 저장소 어디에도 없었다.
> `python-dotenv` 도 `requirements.txt` 에 없다. `.env` 에 값을 적어 두면
> **아무 일도 일어나지 않았다.**
>
> 조용히 위험한 지점은 `run_all.py` 의 `notify()` 다. 토큰이 없으면 그냥
> return 하므로 **실패 알림이 영원히 안 온다.** 무인 운영에서 최악은 조용히
> 멈추는 것인데, 알림 자체가 조용히 죽어 있는 상태였다. T5 에서 물렸을 것이다.
>
> 새 의존성 없이 `src/env.py` 를 만들었다(표준 라이브러리만).
> `threads_post.py` 와 `run_all.py` 가 시작할 때 `load_env()` 를 부른다.
> 시스템 환경변수를 덮어쓰지 않고, 빈 값은 무시한다.
> (`.env.example` 을 그대로 복사해 두면 빈 값들이 "설정됨" 으로 오인된다.)

**검증 순서**
```
python src/threads_post.py --dry-run    # 문구만 확인
python src/threads_post.py              # 실제 발행 1건
```

**완료 기준**
- 실제로 스레드에 게시물이 올라감
- 게시물에 대가성 고지 문구가 포함되어 있음
- `deals.posted_at`이 채워지고 재실행 시 중복 발행되지 않음

**참고**: 개발 모드(development mode)에서 본인 계정만 쓰는 경우
앱 심사 없이 동작할 수 있다. 심사 신청 전에 먼저 시도해 볼 것.

---

## T5. 무인 운영 전환

- [x] 텔레그램 전달 실제 동작 확인 (2026-07-29, 2건 전송)
- [ ] `py src/run_all.py --once --dry-run` 통과
- [ ] `py src/run_all.py --once` 통과
- [ ] 실패 알림이 **실제로 오는지** 테스트 (일부러 실패시켜 확인)
- [ ] Windows 작업 스케줄러 등록 (`tools/install_task.ps1`)
- [ ] PC 재부팅 후 자동으로 살아나는지 확인
- [ ] 카톡 방 창이 닫힌 상태에서 자동으로 여는지 (T1 미검증 항목)

**완료 기준**: 3일 연속 무개입으로 돌아가고, `run.log`에 치명적 오류가 없음

**토큰 갱신은 지금 경로에서 필요 없다.** Threads 자동 발행을 안 쓰기
때문이다. 나중에 켜면 50일마다 `py src/threads_post.py --refresh-token`.

### ⚠️ 반드시 대화형 데스크톱에서 실행해야 한다

이건 설정으로 우회할 수 없다.

| 이유 | 내용 |
|---|---|
| 카톡 자동화 | `pywinauto` 가 실제 창에 키를 보낸다. 화면이 없으면 원리적으로 불가능 |
| 브라우저 | CLAUDE.md 제약 5 가 `headless=False` 를 요구한다 |

따라서 작업 스케줄러에 **"사용자가 로그온한 경우에만 실행"**
(`-LogonType Interactive`) 으로 등록해야 한다. "로그온 여부에 관계없이
실행" 으로 두면 화면 없는 세션에서 돌면서 **조용히 계속 실패한다.**
무인 운영에서 최악의 형태다. `tools/install_task.ps1` 이 이 값으로
고정해 두었다. 바꾸지 말 것.

같은 이유로:
- PC 를 **로그인 상태로** 켜 둘 것
- **절전을 끌 것** (설정 > 시스템 > 전원 > 화면 및 절전 → 전부 '안 함').
  절전에 들어가면 `time.sleep()` 로 대기 중이던 주기가 멈춘다
- **카톡 딜방 창을 열어 둘 것** (방 창 자동 열기가 아직 미검증)

### 등록/해제

```
powershell -ExecutionPolicy Bypass -File tools\install_task.ps1           # 등록
powershell -ExecutionPolicy Bypass -File tools\install_task.ps1 -Status   # 확인
powershell -ExecutionPolicy Bypass -File tools\install_task.ps1 -Remove   # 해제
```

진입점은 `tools/run_bot.cmd` 다. 방 이름을 인자로 주지 않고 `.env` 의
`KAKAO_ROOM` 에서 읽는다. 경로와 방 이름에 공백·한글이 섞이면 스케줄러
인자에서 따옴표가 꼬이기 때문이다.

---

## T6. (승인 후) 공식 API로 전환

쿠팡 파트너스 최종승인 및 API 키 발급 이후.

- [ ] `src/partners_deeplink.py` 신규 작성 — HMAC 인증 + deeplink 엔드포인트 호출
- [ ] `run_all.py`에서 `partners_link.py` 호출을 이것으로 교체
- [ ] `partners_link.py`, `pw_profile/`, `selectors.json` 삭제
- [ ] `playwright` 의존성 제거

여기까지 오면 브라우저 자동화가 사라져 계정 리스크와 불안정성이 동시에 해소된다.
**이게 이 프로젝트의 최종 목표 상태다.**
