requests>=2.31
playwright>=1.44
pywinauto>=0.6.8 ; sys_platform == "win32"

# 스크린샷에서 상품명·가격을 읽는 데 쓴다(src/vision_ocr.py).
# 토스가 웹에서 가격을 빼 버려서 링크만으로는 못 얻는 상품이 있는데,
# 앱 스크린샷에는 다 보이기 때문이다.
# 이것만 없어도 나머지 기능은 전부 동작한다. OCR 경로만 안내문으로 빠진다.
anthropic>=0.116
