# ── 절대 커밋 금지 ────────────────────────────────
# 토큰/세션/자격증명
token.json
.env
pw_profile/
pw_toss_profile/

# 로컬 상태 (기기마다 다름, 개인정보 포함 가능)
deals.db
export.txt
pending.csv
selectors.json
toss_selectors.json
run.log
bot.log
main.log
status.json
shots/
dump_selectors.txt

# 관측 도구의 출력. 계정 정보·발급된 링크가 섞이므로 커밋하지 않는다.
# 도구를 새로 만들 때마다 여기에 한 줄씩 추가하는 걸 잊어서
# 실제로 한 번 커밋된 적이 있다(coupang_issue_observe.txt). 통짜로 막는다.
*_observe.txt
coupang_state.json

# ── 파이썬 ───────────────────────────────────────
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/

# ── 편집기 ───────────────────────────────────────
.vscode/
.idea/
.DS_Store
tg_offset.json
*.lock
