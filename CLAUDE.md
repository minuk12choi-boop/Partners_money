# Partners_money — 프로젝트 브리프

## 목적

카카오톡 딜 공유 오픈채팅방에 올라오는 쿠팡 특가 상품을 자동으로 수집해,
소유자 본인의 쿠팡 파트너스 링크로 변환한 뒤, 소유자의 Threads 계정에 발행한다.

**전 과정 무인 운영이 목표다.** 사람이 개입하는 지점은 최초 1회 로그인뿐이어야 한다.

## 실행 환경

- Windows 10/11, 24시간 켜져 있는 개인 PC
- PC 카카오톡이 로그인된 상태로 상시 실행 중
- Python 3.10+

### ⚠️ 소유자는 **Claude 데스크탑 앱**을 쓴다. 터미널이 아니다.

소유자에게 안내할 때 `cd`, `git pull`, `py src\...` 같은 **명령을 치라고
하지 마라.** 소유자는 터미널을 열지 않는다. 앱에서 대화만 한다.

| 이렇게 쓰지 말 것 | 이렇게 쓸 것 |
|---|---|
| "터미널에서 `git pull` 하세요" | "앱에 '최신으로 맞춰줘' 라고 하세요" — 또는 그냥 내가 실행한다 |
| "`py src\doctor.py` 를 실행하세요" | 내가 실행하고 **결과만** 보여준다 |
| "`cd C:\Users\dkrt\Partners_money`" | 앱에서 그 폴더를 프로젝트로 열면 끝 |

명령이 필요하면 **내가 직접 실행한다.** 소유자에게 넘길 것은 앱이
대신 못 하는 것뿐이다 — 브라우저 로그인, 텔레그램에서 버튼 누르기,
카톡 창 열어 두기 같은 것.

**예외가 하나 있다. `tools\start.cmd` 는 앱 안에서 돌리면 안 된다.**
24시간 떠 있어야 하는데 앱 세션이 끝나면 같이 죽는다. 이건 파일
탐색기에서 **더블클릭**하거나 작업 스케줄러에 등록해서 앱 밖에서
돌린다. 소유자에게 안내할 때 이 구분을 흐리지 마라.

## 데이터 흐름

```
PC 카카오톡 채팅방 창
   │  Ctrl+S (kakao_export.py)
   ▼
export.txt
   │  파싱 · 단축링크 해석 · 중복제거 (kakao_deal_extract.py)
   ▼
deals.db (SQLite)          ← 단일 진실 공급원
   │  본인 파트너스 세션으로 딥링크 생성 (partners_link.py)
   │  토스는 쉐어링크 대시보드에서 발급 (toss_link.py)
   ▼
deals.affiliate_url 채워짐
   │  문구 생성 · 텔레그램 전달 (telegram_deliver.py)
   ▼
텔레그램 — **/start 한 구독자 전원**
   │  사람이 보고 골라서 복사 → 붙여넣기
   ▼
Threads
```

`run_all.py`가 위 단계를 20분 주기로 반복 호출한다.
`main.py`가 그 `run_all.py`와 텔레그램 봇(`telegram_bot.py`)을 함께 띄우고,
어느 쪽이 죽어도 다시 살린다. **평소 실행은 `tools\start.cmd` 하나다.**

### 누가 무엇을 할 수 있나 (2026-08-02 갱신)

| 구분 | 누구 | 할 수 있는 것 |
|---|---|---|
| 구독자 | 봇에 `/start` 한 사람 누구나 | 문구를 **받는다** + 링크를 보내 **변환을 시킨다** |
| 관리자 | `TG_CHAT_ID` + `TG_ADMIN_IDS` | 위 전부 + 구독자 목록 조회(`/subs`) |

**2026-08-02 소유자 지시로 변환 권한을 구독자 전원에게 열었다.**
전에는 관리자만이었다. "어차피 아는 사람이 쓴다" 는 판단이고, 누가
쓰느냐는 소유자가 정할 일이다.

⚠️ **권한은 열렸지만 속도는 열리지 않았다. 이걸 같이 풀지 마라.**
변환 한 번이 곧 소유자 PC 의 브라우저로 쿠팡 파트너스·토스에 접속하는
것이다. 사람이 열 명이면 접근도 열 배가 되고, 그러면 아래 **제약 2 가
사람 손으로 깨진다.** 제약 2 는 완화 대상이 아니다.

그래서 `subscribers.convert_allowed()` 가 총량으로 묶는다. 누가 시키든
사이트에 닿는 빈도는 일정하다.

| 값 | 기본 | 뜻 |
|---|---|---|
| `CONVERT_PER_HOUR` | 20 | 전체 합계. 관리자도 포함 |
| `CONVERT_PER_HOUR_EACH` | 8 | 1인당. 한 사람이 총량을 독차지하지 못하게 |
| `CONVERT_MIN_GAP` | 6초 | 연타 방지. 제약 2 의 `sleep(6)` 과 같은 취지 |

`.env` 로 조절할 수 있지만 **올릴 때는 제약 2 를 다시 읽을 것.**

받는 것은 누구나 안전하다 — 링크는 어차피 소유자 것이고 많이 퍼질수록
수익이 는다.

### ⚠️ 마지막 단계는 자동이 아니다 (2026-07-29 소유자 결정)

원래는 `threads_post.py`가 Threads API로 자동 발행하는 설계였다.
**소유자가 사람이 직접 올리는 방식으로 바꿨다.** 파이프라인은 '링크 +
완성된 본문'을 텔레그램으로 보내는 데까지만 한다.

- Meta 앱 심사 리스크가 통째로 사라진다
- 무엇을 언제 올릴지는 사람이 정한다
- `threads_post.py`는 **지우지 않았다.** 나중에 자동 발행을 켤 때 쓴다.
  `build_text()`는 `telegram_deliver.py`가 그대로 import 해서 쓰므로
  고지 문구 규칙의 단일 출처로 계속 살아 있다.

`deals.sent_at` = 텔레그램으로 보낸 시각.
`deals.posted_at` = 실제 Threads 발행 시각(지금은 안 쓴다).
**둘을 섞지 마라.** '보냈다'와 '올라갔다'는 다른 사실이고, 후자는
프로그램이 알 수 없다.

### deals 테이블

| 컬럼 | 의미 |
|---|---|
| `platform` | `coupang` 또는 `toss`. 딜방은 두 플랫폼을 섞어 올린다 |
| `product_id` | 쿠팡 productId 또는 토스 상품ID |
| `product_url` | 단축링크를 해석해 얻은 실제 상품 URL (파라미터 정리됨) |
| `affiliate_url` | 내 제휴 링크. NULL이면 아직 미생성 |
| `sent_at` | 텔레그램으로 보낸 시각. NULL이면 아직 안 보냄 |
| `posted_at` | 실제 Threads 발행 시각. **지금 경로에서는 안 채워진다** |

**중복 판정의 기준 키는 `(platform, product_id)` 복합 키다.**
둘 다 숫자 ID라 단독으로는 충돌할 수 있다 (쿠팡 `2279371037`, 토스 `524516537`).

`sent_at` 은 **한 명에게라도 갔으면** 채운다. '오늘 N번째' 표시와 리포트에
쓰인다.

### ⚠️ `sent_at` 으로 보낼지 말지를 판정하지 마라 (2026-08-02 실측)

**받았는지는 사람마다 다른 사실이다.** 딜에 한 번만 적으면 안 된다.

전에는 `load_pending()` 이 `sent_at IS NULL` 로 걸렀다. 그래서 딜 하나가
**누구에게든** 나가는 순간 잠기고, 그 뒤에 `/start` 한 사람은 그 딜을
영원히 못 받았다. 실제로 구독자 1명이 08:58 에 가입했는데 링크 44건이
전부 08:55 이전에 잠겨 있어 **0건**을 받았다. 소유자에게만 가고 구독자에게는
아무것도 안 갔다.

판정은 `deliveries` 테이블이 한다.

### deliveries 테이블

| 컬럼 | 의미 |
|---|---|
| `(platform, product_id, chat_id)` | 복합 기본키. 누가 무엇을 받았는가 |
| `sent_at` | 그 사람에게 간 시각 |

`pick_targets()` 가 두 가지를 건다 — 이미 받은 사람은 빼고, 가입 전에
올라온 딜은 보내지 않는다. 소유자(`TG_CHAT_ID`)는 `subscribers` 에 없을 수
있어 가입 시각을 모르므로 거르지 않는다.

**새로 가입한 사람에게 지난 딜을 얼마나 줄지는 양 끝이 다 나쁘다.**
전부 주면 첫인사가 44통 스팸이라 차단당하고, 안 주면 가입 직후 0건이라
고장으로 보인다. `WELCOME_CATCHUP=3` 으로 최근 3건만 준다.

### subscribers 테이블

봇에 `/start` 한 사람들. `telegram_bot.py`가 채우고
`telegram_deliver.py`가 읽는다.

| 컬럼 | 의미 |
|---|---|
| `chat_id` | 텔레그램 대화 ID |
| `active` | 0이면 안 보낸다. `/stop` 이거나 봇을 차단당했을 때 |
| `last_error` | 왜 껐는지. 차단당한 경우 텔레그램이 준 문구가 들어간다 |

차단당한 구독자는 **지우지 않고 끈다.** 지우면 다음 주기에 또 보내려다
또 403 을 받고, 왜 안 가는지도 안 남는다. 일시적인 오류(429 등)로는
끄지 않는다 — 그러면 멀쩡한 구독자가 조용히 사라진다.

`pending.csv`는 **사람이 눈으로 보는 용도**다. 파이프라인은 `deals.db`만 읽는다.

---

## ⚠️ 이 코드의 검증 상태 — 가장 먼저 읽을 것

이 저장소의 코드는 **Windows, 카카오톡, 쿠팡 파트너스, Threads API를 한 번도 실제로 만난 적이 없다.**
문법과 파싱 로직만 검증됐다. 추측으로 작성된 부분이 명확히 존재한다.

| 파일 | 상태 | 추측으로 작성된 부분 |
|---|---|---|
| `kakao_deal_extract.py` | 🟢 샘플 txt로 검증됨 | 실제 방의 메시지 포맷은 미확인 |
| `threads_post.py` | 🟡 문구 생성만 검증됨 | API 호출 경로 전체 미검증 |
| `run_all.py` | 🟡 구조만 | — |
| `partners_link.py` | 🔴 **완전 미검증** | 파트너스 페이지 DOM 전체 |
| `kakao_export.py` | 🔴 **완전 미검증** | 카톡 윈도우 클래스명, 저장 대화상자 동작 |

**따라서 최우선 작업은 기능 추가가 아니라 실제 환경에서 하나씩 돌려보며 고치는 것이다.**
동작하지 않는 부분을 발견하면 추측으로 덮지 말고, 실제 값을 관측한 뒤 고쳐라.

---

## 🚫 절대 완화하면 안 되는 제약

이건 취향이 아니라 계정이 정지되거나 수익이 몰수되는 조건이다.
"효율" 을 이유로 아래를 조정하지 마라.

1. **대가성 고지 문구는 모든 게시물에 반드시 포함된다.**
   500자 초과 시에도 이 문구는 자르지 않고 상품명 쪽을 줄인다.
   누락 시 `assert`로 발행을 중단시키는 로직이 있다. 제거 금지.

   **플랫폼마다 문구도 위치도 다르다. 섞어 쓰면 양쪽 다 위반이다.**

   | 플랫폼 | 상수 | 위치 |
   |---|---|---|
   | 쿠팡 | `DISCLOSURE` | 본문 끝 |
   | 토스 | `TOSS_DISCLOSURE` | **본문 첫 부분** (토스 운영정책 요구) |

   `build_text()`가 고지 누락, 타 플랫폼 문구 혼입, 토스 고지 위치를
   각각 `assert`로 막는다. 셋 다 제거 금지.

2. **파트너스 사이트 접근 빈도를 올리지 마라.**
   `MAX_LINKS_PER_CYCLE = 4`, 링크 생성 사이 `time.sleep(6)`.
   이건 자동화 탐지를 피하기 위한 값이다. 병렬화·헤드리스 전환·대기시간 단축 전부 금지.

3. **발행량 조절 — 2026-07-29 소유자 결정으로 책임이 사람에게 옮겨갔다.**

   원래 조항은 "하루 5건 캡을 유지한다" 였다. 이유는 지금도 유효하다.
   API 한도는 250건이지만 **링크 위주 계정은 도달이 급락한다.**

   마지막 단계가 자동 발행에서 텔레그램 전달로 바뀌면서, 이 캡을 코드가
   강제할 자리가 없어졌다. 실제로 무엇을 몇 건 올릴지는 사람이 정한다.

   | 경로 | 캡 |
   |---|---|
   | `telegram_deliver.py` (지금 쓰는 것) | **없음.** 소유자가 골라서 올린다 |
   | `threads_post.py` (자동 발행, 지금은 미사용) | `DAILY_CAP=5`, `MIN_GAP_MINUTES=90` **유지. 건드리지 말 것** |

   `telegram_deliver.py`는 메시지마다 "오늘 N번째"를 찍는다. 캡을 강제하지는
   않지만 몰아서 올리고 있다는 것은 보이게 한다.

   **몰아서 올리면 도달이 떨어지는 것은 자동이든 수동이든 같다.**
   자동 발행을 다시 켤 때는 위 두 값을 그대로 되살릴 것.

4. **파트너스 링크를 URL 파라미터로 직접 조립하지 마라.**
   손으로 만든 링크는 클릭은 되지만 트래킹이 안 붙어 수익이 0으로 찍힌다.
   반드시 파트너스 시스템(또는 승인 후 공식 API)이 반환한 링크만 사용한다.

5. **`playwright`는 `headless=False`를 유지한다.** 헤드리스는 탐지 위험이 크다.

---

## 프로젝트의 현재 단계

**Phase 0 — 지금.** 쿠팡 파트너스 API 키가 없다.
API 키는 "최종승인" 파트너에게만 발급되며, 링크를 통한 누적 판매 15만 원이 필요하다.
그래서 `partners_link.py`가 브라우저 자동화로 임시 다리 역할을 한다.

**Phase 1 — 승인 후.** `partners_link.py`를 통째로 버리고
공식 deeplink API(HMAC 인증) 호출로 대체한다. 이게 최종 목표 상태다.
브라우저 자동화가 사라지므로 안정성과 계정 리스크가 동시에 해결된다.

→ **`partners_link.py`에 과투자하지 마라. 폐기될 코드다.** 동작하는 수준까지만.

---

## 코딩 규약

- 표준 라이브러리 + `requirements.txt`에 있는 것만. 새 의존성 추가 전에 물어볼 것.
- 모든 상태는 `deals.db`에 저장한다. 중간 파일에 상태를 두지 마라.
- 실패는 조용히 넘기지 말고 로그에 남긴다. **무인 운영에서 최악은 조용히 멈추는 것이다.**
- 비밀값은 환경변수 또는 `token.json`. 코드에 하드코딩 금지. `.gitignore` 확인할 것.
- 주석과 로그 메시지는 한국어.

## 소유자에게 확인해야 할 것

다음은 코드로 알아낼 수 없다. 막히면 추측하지 말고 물어봐라.

- 카톡 채팅방의 정확한 이름
- 그 방의 실제 메시지 포맷 (`export.txt` 샘플 몇 줄)
- Threads 앱 ID / 리디렉션 URI 등록 여부
- 쿠팡 파트너스 최종승인 진행 상황
